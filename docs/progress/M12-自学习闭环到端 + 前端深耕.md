---
title: M12 自学习闭环到端 + 前端深耕 进度快照
milestone: M12
version: v1
date: 2026-04-30
commits: 4
tests: +13 backend / +13 frontend
hours: ~5
opus-estimate: 16-20h
savings: ~70%
status: completed
tags: [kap, m12]
---

> 进程链：[M0 KAP-Lite](M0-KAP-Lite.md) → ... → [M11 自学习闭环与 GT 工作流](M11-自学习闭环与GT工作流.md) → **【M12 自学习闭环到端 + 前端深耕】** → M13⬜
>
> 设计蓝本：[决策书 §5.3 prompt 自学习真闭环 + 前端可视化深化](../01-技术决策书.md)

# M12 自学习闭环到端 + 前端深耕（4 commits / ~5h vs Opus 估 16-20h，节省 ~70%）

## 全景成果

### M12 #1 · LLM 自学习闭环完整版
- PromptVersion 加 `system_prompt` 字段（不截断，完整存）
- `resolve_active_system_prompt(condition_type, fallback)` 工具
- evolution_proposer 4 函数接入：propose_new_entity_type / relation_solidification / relation_split / standard_upgrade
- 闭环：SME 切换 PromptVersion → system_prompt 立即生效 → 后续 proposals 用新 prompt → M11 #4 时间窗 AB 比较自动归类
- 测试 +6 (TestResolveActiveSystemPrompt)
- **关联代码**：[`packages/observability/prompt_versions.py`](../../backend/packages/observability/prompt_versions.py) + [`packages/ontology/evolution_proposer.py`](../../backend/packages/ontology/evolution_proposer.py)

### M12 #2 · PromptVersion PG 持久化 + portal feedback UI
后端：
- `pg_prompt_versions` 模块（write-through，同 pg_decision_log/pg_query_log/pg_recall_eval 模式）
- DDL `prompt_versions` (PK + 7 字段) + INDEX (condition_type, activated_at DESC)
- 双 sink (upsert / deactivate) 注入 prompt_versions 模块
- 环境变量 KAP_PROMPT_VER_PG=1 启用
- AskResponse 加 `query_id` 字段（portal 反馈用）

前端：
- `QueryFeedbackButton` 组件（紧凑 / 文字双模式 + 已反馈高亮）
- QAPage 集成：assistant 消息下挂 thumbs up/down
- services 加 `submitQueryFeedback`

测试 +12（后端 7 PG + 前端 5 button）
- **关联代码**：[`packages/observability/pg_prompt_versions.py`](../../backend/packages/observability/pg_prompt_versions.py) + [`frontend/src/components/v15/QueryFeedbackButton.tsx`](../../frontend/src/components/v15/QueryFeedbackButton.tsx)

### M12 #3 · 块② 4×6 矩阵审核 UI 收尾
GovernanceMatrix 组件已较完整（4×6 grid + drawer + claim/decide/escalate）。
M12 #3 补 smoke test 进入"已交付 + 已测"状态：
- 4 测试（角色/工位渲染 + 总数/单元格 count + R/C/I 图例 + 刷新按钮）
- **关联代码**：[`frontend/src/pages/v15/GovernanceMatrix.test.tsx`](../../frontend/src/pages/v15/GovernanceMatrix.test.tsx)

### M12 #4 · Dashboard 趋势曲线图
- 新依赖 `recharts` ^3.8
- `RecallTrendChart` 组件：avg_recall (绿) / avg_precision (蓝) / avg_f1 (黄) 三条线
- ResponsiveContainer + 自定义深色 Tooltip + Y 轴百分比格式化
- ObservabilityDashboard 集成：6 卡片下方完整宽度图表
- 4 smoke 测试（loading / empty / 渲染 / error）
- **关联代码**：[`frontend/src/components/v15/RecallTrendChart.tsx`](../../frontend/src/components/v15/RecallTrendChart.tsx)

---

## Commits 时间线

| Commit    | 内容                                                              | 测试           |
|:---|:---|:---:|
| `bb31f09` | #1 LLM 自学习闭环完整版（system_prompt + resolve + 4 函数接入）   | +6 ✓           |
| `?` (#2)  | #2 PromptVersion PG + portal feedback UI                          | +7 后 + 5 前   |
| `28a6a0f` | #3 GovernanceMatrix smoke test                                    | +4 前          |
| `?` (#4)  | #4 Dashboard 趋势曲线图（recharts + RecallTrendChart）            | +4 前          |

---

## 测试基线

`941/943 unit ✓` 后端 + `21 frontend tests passed`（连接器 mock 数 2 个 pre-existing 不变）。

---

## M12 已交付

- ✓ LLM 自学习闭环到端（SME 切 prompt → 立即生效 → AB 自动归类）
- ✓ PromptVersion PG 持久化（跨重启不丢配置）
- ✓ portal "有用 / 无用" 反馈 UI
- ✓ 4×6 矩阵 UI smoke test 收尾
- ✓ Dashboard 召回评估历史曲线图

---

## M13 待启动方向

**项目反馈新约束**（M12 收尾时由用户提出）：
- ⚠️ 涉及 LLM 的测试改用真实 LLM（不 mock acall_llm_json）
- 老 mock 测试不强制重写；新增 LLM 测试遵循此原则
- 需要在 ``pyproject.toml`` 加 ``@pytest.mark.live_llm`` 标记 + CI 默认 skip 配置

其他 M13 候选：
- 真 LLM 集成测试基建（pytest 标记 + KAP_LLM_API_BASE 环境变量 + 弱断言）
- ChunkHashCache LRU 大规模分片（> 1M chunks 优化）
- DecisionLog / QueryLog PG 时序分区（按月分区）
- 独立物理 Neo4j 实例的影子库（生产部署优化）
- ISS-Job Quartz 真接入（替代 HTTP 端点）
- 前端运营仪表盘加历史看板对比（多 project 横评）
- 块②前端 GovernanceMatrix 补"我认领的"快速过滤 + bulk actions
